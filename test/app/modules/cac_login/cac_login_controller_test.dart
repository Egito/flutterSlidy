import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:flutterSlidy/app/modules/cac_login/cac_login_controller.dart';
import 'package:flutterSlidy/app/modules/cac_login/cac_login_module.dart';

void main() {
  initModule(CacLoginModule());
  // CacLoginController caclogin;
  //
  setUp(() {
    //     caclogin = CacLoginModule.to.get<CacLoginController>();
  });

  group('CacLoginController Test', () {
    //   test("First Test", () {
    //     expect(caclogin, isInstanceOf<CacLoginController>());
    //   });

    //   test("Set Value", () {
    //     expect(caclogin.value, equals(0));
    //     caclogin.increment();
    //     expect(caclogin.value, equals(1));
    //   });
  });
}
