import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:flutterSlidy/app/modules/cac_login/cac_login_page.dart';

void main() {
  testWidgets('CacLoginPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(CacLoginPage(title: 'CacLogin')));
    //  final titleFinder = find.text('CacLogin');
    //  expect(titleFinder, findsOneWidget);
  });
}
